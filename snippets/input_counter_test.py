def inputTotal():
	total = 0
	entry = input("Enter a value or q to quit")
	while entry != "q":
		total += 1
		print(total)
		entry = input("Enter a value or q to quit")
		
	else:
		total == "q"
		print("Goodbye")
		
